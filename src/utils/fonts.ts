import { Inter, Oxanium, Quicksand} from "next/font/google"

export const inter = Inter({
    subsets: ['latin'],
    display: "swap"
})

export const oxanium = Oxanium({
    subsets: ['latin'],
    display: "swap"
})

export const quicksand = Quicksand({
    subsets: ['latin'], 
    display: "swap"
})
